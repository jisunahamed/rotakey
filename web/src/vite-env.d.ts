/// <reference types="vite/client" />

// Vite's ambient types, for `import.meta.env` — which the console reads in exactly
// one place: the guard that keeps the kitchen sink out of a production build.
